services:
  db:
    image: postgres:17-alpine
    container_name: logline-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: logline
      POSTGRES_PASSWORD: password
      POSTGRES_DB: logline
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
